import { Box } from "@mui/system";
import {styled} from "@mui/system"

//custom element created by styled
const FlexBetween = styled(Box)({
    display:"flex",
    justifyContent:"space-between",
    alignItems:"center"
})
export default FlexBetween;